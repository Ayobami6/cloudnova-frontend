# Base URL of the cloudnova-api Django backend, including the /api/v1 prefix.
# The backend's dev server listens on :8000 by default (`make run` / `manage.py
# runserver 0.0.0.0:8000`).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
